spring:
  datasource:
    url: jdbc:mysql://mysql-container/gee?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: madhu
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mvc:
    view:
      prefix: /views/
      suffix: .jsp
